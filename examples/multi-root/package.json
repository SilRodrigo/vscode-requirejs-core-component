{
  "name": "multi-root",
  "description": "Application maintained with a multi-root workspace.",
  "private": true,
  "scripts": {
    "build": "cd nature && npm run build && cd ../garden && npm run build"
  }
}
